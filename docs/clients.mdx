---
title: "示例客户端"
description: "支持 MCP 集成的应用程序列表"
---

{/* prettier-ignore-start */}

export const FEATURES = ["Resources", "Prompts", "Tools", "Discovery", "Instructions", "Sampling", "Roots", "Elicitation", "CIMD", "DCR", "OAuth Client Credentials", "Enterprise-Managed Authorization", "Tasks", "Apps"];

export const FEATURE_COLORS = {
  Resources: "blue",
  Prompts: "blue",
  Tools: "blue",
  Instructions: "purple",
  Discovery: "purple",
  Sampling: "green",
  Roots: "green",
  Elicitation: "green",
  Tasks: "orange",
  Apps: "orange",
  DCR: "yellow",
  CIMD: "yellow",
  "OAuth Client Credentials": "yellow",
  "Enterprise-Managed Authorization": "yellow",
};

export const FeatureBadge = ({ feature }) => {
  const color = FEATURE_COLORS[feature.split(" (")[0]] || "gray";
  return <Badge shape="pill" stroke color={color}>{feature}</Badge>;
};

export const filterStore = {
  state: {
    selectedFeatures: [],
    searchText: "",
    visibleCount: 0,
    totalCount: 0
  },
  listeners: new Set(),
  setState(updater) {
    if (typeof updater === "function") {
      this.state = { ...this.state, ...updater(this.state) };
    } else {
      this.state = { ...this.state, ...updater };
    }
    this.listeners.forEach(fn => fn(this.state));
  },
  subscribe(fn) {
    this.listeners.add(fn);
    return () => this.listeners.delete(fn);
  }
};

export const useFilterStore = () => {
  const [state, setState] = useState(filterStore.state);
  useEffect(() => filterStore.subscribe(setState), []);
  return state;
};

export const useFilter = ({ name, supports }) => {
  const { selectedFeatures, searchText } = useFilterStore();

  const isVisible = (
    name.toLowerCase().includes(searchText.toLowerCase()) &&
    selectedFeatures.every(feature => supports?.includes(feature))
  );

  useEffect(() => {
    filterStore.setState(s => ({ totalCount: s.totalCount + 1 }));
    return () => filterStore.setState(s => ({ totalCount: s.totalCount - 1 }));
  }, []);

  useEffect(() => {
    if (isVisible) {
      filterStore.setState(s => ({ visibleCount: s.visibleCount + 1 }));
      return () => filterStore.setState(s => ({ visibleCount: s.visibleCount - 1 }));
    }
  }, [isVisible]);

  return isVisible;
};


export const ClientFilter = () => {
  const { selectedFeatures, searchText, visibleCount, totalCount } = useFilterStore();

  useEffect(() => {
    filterStore.setState({ selectedFeatures: [], searchText: "" });
  }, []);

  const toggleFeature = (feature) => {
    const newFeatures = selectedFeatures.includes(feature)
      ? selectedFeatures.filter(f => f !== feature)
      : [...selectedFeatures, feature];
    filterStore.setState({ selectedFeatures: newFeatures });
  };

  const clearFilters = () => {
    filterStore.setState({ selectedFeatures: [], searchText: "" });
  };

  const hasFilters = selectedFeatures.length > 0 || searchText.length > 0;

  return (
    <div className="p-4 border border-gray-200 dark:border-gray-700 rounded-lg bg-gray-50 dark:bg-gray-800/50">
      <div className="flex items-center justify-between">
        <span className="font-bold text-gray-700 dark:text-gray-300">
          Showing {visibleCount} of {totalCount} clients
        </span>
        {hasFilters && (
          <button
            onClick={clearFilters}
            className="text-sm cursor-pointer px-3 py-1 rounded-full bg-gray-200 dark:bg-gray-700 text-gray-700 dark:text-gray-300 hover:bg-gray-300 dark:hover:bg-gray-600 transition-colors"
          >
            <Icon icon="xmark" iconType="solid" size={16} /> Clear filters
          </button>
        )}
      </div>
      <div className="mt-3">
        <input
          type="text"
          placeholder="Search clients by name..."
          value={searchText}
          onChange={(e) => filterStore.setState({ searchText: e.target.value })}
          className="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100 placeholder-gray-500 dark:placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent"
        />
      </div>
      <div className="mt-4">
        <div className="text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
          Filter by features:
        </div>
        <div className="flex flex-wrap gap-2">
          {FEATURES.map(feature => (
            <button
              key={feature}
              onClick={() => toggleFeature(feature)}
              className={`flex items-center gap-1.5 px-2 py-1 rounded text-sm transition-colors cursor-pointer ${
                selectedFeatures.includes(feature)
                  ? 'bg-primary/10 text-primary dark:bg-gray-700 dark:text-gray-100'
                  : 'text-gray-600 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800'
              }`}
            >
              <Icon
                icon={selectedFeatures.includes(feature) ? "square-check" : "square"}
                iconType={selectedFeatures.includes(feature) ? "solid" : "regular"}
                size={16}
              />
              {feature}
            </button>
          ))}
        </div>
      </div>
    </div>
  );
};


export const McpClient = ({ name, homepage, supports, sourceCode, instructions, children }) => {
  const slug = name
    .toLowerCase()
    .replace(/[().\s-]+/g, "-")
    .replace(/^-|-$/g, "");

  if (homepage?.match(/^https:\/\/github\.com\/[^/]+\/[^/]+/)) {
    sourceCode ??= homepage;
  }

  const features = (supports ?? "").split(", ").sort((a, b) => {
    const featureA = a.split(" (")[0];
    const featureB = b.split(" (")[0];
    return FEATURES.indexOf(featureA) - FEATURES.indexOf(featureB);
  });

  const instructionsLinks = Array.isArray(instructions)
    ? <>
        <strong>Configuration instructions:</strong>{" "}
        {instructions.map(([text, url], i) => [
          i > 0 && ", ",
          <a href={url} target="_blank" rel="noopener noreferrer">{text}</a>
        ])}
      </>
    : <a href={instructions} target="_blank" rel="noopener noreferrer">
        Configuration instructions
      </a>;

  const [expanded, setExpanded] = useState(false);
  const [hasOverflow, setHasOverflow] = useState(false);
  const contentRef = useRef(null);
  const isVisible = useFilter({ name, supports });

  useEffect(() => {
    const el = contentRef.current;
    if (el) {
      setHasOverflow(el.scrollHeight > el.clientHeight);
    }
  }, []);

  if (!isVisible) return null;

  return (
    <div id={slug} className="group mt-8 scroll-mt-32">
      <div className="border border-gray-200 dark:border-gray-700 rounded-lg overflow-hidden">
        <div className="px-4 py-3 border-b border-gray-200 dark:border-gray-700">
          <div className="flex items-center gap-2">
            <a
              href={homepage}
              className="border-0 text-xl font-semibold text-primary underline"
              target="_blank"
              rel="noopener noreferrer"
            >
              {name}
            </a>
            <a
              href={`#${slug}`}
              className="ml-auto border-0 opacity-0 group-hover:opacity-100 text-xl text-gray-400 hover:text-gray-600"
              aria-label={`Link to ${name}`}
            >
              #
            </a>
          </div>
          {features.length > 0 && (
            <div className="flex items-baseline gap-1.5 mt-2 text-base">
              <span className="inline-flex items-center h-[1lh]">
                {'\u200B'}<Icon icon="check" iconType="solid" size={18} />
              </span>
              <strong>Supports:</strong>
              <span className="flex flex-wrap gap-1">
                {features.map(feature => (
                  <FeatureBadge key={feature} feature={feature} />
                ))}
              </span>
            </div>
          )}
          {sourceCode && (
            <div className="flex items-baseline gap-1.5 mt-2 text-base">
              <span className="inline-flex items-center h-[1lh]">
                {'\u200B'}<Icon icon="code" iconType="solid" size={18} />
              </span>
              <span>
                <a href={sourceCode} target="_blank" rel="noopener noreferrer">
                  Open source
                </a>
              </span>
            </div>
          )}
          {instructions && (
            <div className="flex items-baseline gap-1.5 mt-2 text-base">
              <span className="inline-flex items-center h-[1lh]">
                {'\u200B'}<Icon icon="gear" iconType="solid" size={18} />
              </span>
              <span>
                {instructionsLinks}
              </span>
            </div>
          )}
        </div>
        <div className="relative">
          <div
            ref={contentRef}
            className={`px-4 py-4 prose ${!expanded ? "max-h-[7rem] overflow-hidden" : "pb-8"}`}
          >
            {children}
          </div>
          {hasOverflow && (
            <button
              onClick={() => setExpanded(!expanded)}
              className={`absolute bottom-0 left-0 right-0 flex justify-center items-end pb-1 cursor-pointer text-gray-400 hover:text-gray-600 ${
                !expanded ? "h-16 bg-gradient-to-t from-white dark:from-gray-900 to-transparent" : "h-8"
              }`}
            >
              <span className={`${expanded ? "rotate-180" : ""} bg-white/60 dark:bg-gray-900/60 rounded-full`}>
                <Icon icon="chevron-down" iconType="solid" size={18} />
              </span>
            </button>
          )}
        </div>
      </div>
    </div>
  );
};

{/* prettier-ignore-end */}

本页面展示了支持模型上下文协议 (MCP) 的应用程序。每个客户端可能支持不同的 MCP 功能：

| 功能                                                      | 描述                                                                                                     |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| <FeatureBadge feature="Resources" />                        | 服务器暴露的数据和内容                                                                              |
| <FeatureBadge feature="Prompts" />                          | 用于 LLM 交互的预定义模板                                                                   |
| <FeatureBadge feature="Tools" />                            | LLM 可以调用的可执行函数                                                                    |
| <FeatureBadge feature="Discovery" />                        | 支持工具/提示词/资源变更通知                                                    |
| <FeatureBadge feature="Instructions" />                     | 服务器为 LLM 提供的指导                                                                            |
| <FeatureBadge feature="Sampling" />                         | 服务器发起的 LLM 补全                                                                             |
| <FeatureBadge feature="Roots" />                            | 文件系统边界定义                                                                              |
| <FeatureBadge feature="Elicitation" />                      | 用户信息请求                                                                                    |
| <FeatureBadge feature="CIMD" />                             | [客户端 ID 元数据文档](specification/latest/basic/authorization#client-id-metadata-documents) 支持 |
| <FeatureBadge feature="DCR" />                              | [动态客户端注册](specification/latest/basic/authorization#dynamic-client-registration) 支持  |
| <FeatureBadge feature="OAuth Client Credentials" />         | [OAuth 客户端凭证](/extensions/auth/oauth-client-credentials) 扩展支持                      |
| <FeatureBadge feature="Enterprise-Managed Authorization" /> | [企业托管授权](/extensions/auth/enterprise-managed-authorization) 扩展支持      |
| <FeatureBadge feature="Tasks" />                            | 长时间运行操作跟踪                                                                              |
| <FeatureBadge feature="Apps" />                             | 交互式 HTML 界面                                                                                  |

<Note>

此列表由社区维护。如果您发现任何不准确之处，或者想要添加或更新有关您的应用程序中 MCP 支持的信息，请 [提交拉取请求](https://github.com/modelcontextprotocol/modelcontextprotocol/pulls)。

</Note>

## 客户端详情

<ClientFilter />

<McpClient
  name="5ire"
  homepage="https://github.com/nanbingxyz/5ire"
  supports="Tools"
>

5ire 是一个开源跨平台桌面 AI 助手，支持通过 MCP 服务器使用工具。

**主要功能：**

- 内置 MCP 服务器可以快速启用和禁用。
- 用户可以通过修改配置文件添加更多服务器。
- 它是开源且用户友好的，适合初学者。
- 未来对 MCP 的支持将持续改进。

</McpClient>

<McpClient
  name="AgentAI"
  homepage="https://github.com/AdamStrojek/rust-agentai"
  supports="Tools"
>

AgentAI 是一个旨在简化 AI 代理创建的 Rust 库。该库包含与 MCP 服务器的无缝集成。

**主要功能：**

- 多 LLM 支持 – 我们支持大多数 LLM API（OpenAI、Anthropic、Gemini、Ollama 以及所有兼容 OpenAI API 的服务）。
- 内置支持 MCP 服务器。
- 在像 Rust 这样类型安全和内存安全的语言中创建代理流程。

**了解更多：**

- [MCP 服务器集成示例](https://github.com/AdamStrojek/rust-agentai/blob/master/examples/tools_mcp.rs)

</McpClient>

<McpClient
  name="AgenticFlow"
  homepage="https://agenticflow.ai/"
  supports="Resources, Prompts, Tools, Discovery"
>

AgenticFlow 是一个无代码 AI 平台，帮助您构建全天候处理销售、营销和创意任务的代理。通过 MCP 安全地连接 2,500+ API 和 10,000+ 工具。

**主要功能：**

- 无代码 AI 代理创建和工作流构建。
- 通过 MCP 访问庞大的 10,000+ 工具和 2,500+ API 库。
- 连接 MCP 服务器的简单 3 步流程。
- 安全管理连接并随时撤销访问权限。

**了解更多：**

- [AgenticFlow MCP 集成](https://agenticflow.ai/mcp)

</McpClient>

<McpClient
  name="AIQL TUUI"
  homepage="https://github.com/AI-QL/tuui"
  supports="Resources, Prompts, Tools, Discovery, Sampling, Elicitation"
>

AIQL TUUI 是一个原生跨平台桌面 AI 聊天应用程序，支持 MCP。它支持多个 AI 提供商（例如 Anthropic、Cloudflare、Deepseek、OpenAI、Qwen）、本地 AI 模型（通过 vLLM、Ray 等）和聚合 API 平台（如 Deepinfra、Openrouter 等）。

**主要功能：**

- **动态 LLM API 和代理切换**：无缝切换不同的 LLM API 和代理。
- **全面的功能支持**：内置支持工具、提示词、资源和采样方法。
- **可配置的代理**：通过代理设置提供可选择和可自定义的工具，增强灵活性。
- **高级采样控制**：修改采样参数并利用多轮采样以获得最佳结果。
- **跨平台兼容性**：完全兼容 macOS、Windows 和 Linux。
- **免费且开源 (FOSS)**：宽松的许可允许修改和自定义应用程序捆绑。

**了解更多：**

- [TUUI 文档](https://www.tuui.com/)
- [AIQL GitHub 仓库](https://github.com/AI-QL)

</McpClient>

<McpClient
  name="Amazon Q CLI"
  homepage="https://github.com/aws/amazon-q-developer-cli"
  supports="Prompts, Tools"
>

Amazon Q CLI 是一个开源的、用于终端的代理编码助手。

**主要功能：**

- 全面支持 MCP 服务器。
- 使用您喜欢的文本编辑器编辑提示词。
- 使用 `@` 即时访问保存的提示词。
- 直接从终端控制和组织 AWS 资源。
- 工具、配置文件、上下文管理、自动压缩等等！

**开始使用**

```bash
brew install amazon-q
```

</McpClient>

<McpClient
  name="Amazon Q IDE"
  homepage="https://aws.amazon.com/q/developer"
  supports="Tools"
  instructions="https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/mcp-ide.html"
>

Amazon Q IDE 是一个开源的、用于 IDE 的代理编码助手。

**主要功能：**

- 支持 VSCode、JetBrains、Visual Studio 和 Eclipse IDE。
- 直接从 IDE 控制和组织 AWS 资源。
- 通过 IDE 用户界面管理每个 MCP 工具的权限。

</McpClient>

<McpClient
  name="Amp"
  homepage="https://ampcode.com"
  supports="Resources, Prompts, Tools, Sampling"
  instructions="https://ampcode.com/manual#mcp"
>

Amp 是由 Sourcegraph 构建的代理编码工具。它在 VS Code（以及兼容的分支如 Cursor、Windsurf 和 VSCodium）、JetBrains IDEs、Neovim 中运行，也可作为命令行工具使用。它还支持多人协作——您可以共享线程并与团队协作。

**主要功能：**

- 对启用的工具和权限进行细粒度控制
- 支持在 VS Code `mcp.json` 中定义的 MCP 服务器

</McpClient>

<McpClient
  name="Apidog"
  homepage="https://apidog.com"
  supports="Resources, Prompts, Tools"
  instructions="https://docs.apidog.com/mcp-client-1930835m0"
>

Apidog 是一个多合一的 API 开发和测试平台，具有内置的 MCP 客户端，专为调试和测试 MCP 服务器而设计。

**主要功能：**

- **全功能支持**：通过用户友好的 GUI 调试 MCP 服务器的工具、提示词和资源。
- **双传输模式**：支持本地进程的 STDIO 和远程服务器的 HTTP。
- **易于设置**：自动解析 MCP 配置文件，支持直接命令或 URL 输入。
- **身份验证**：支持 OAuth 2.0、API Key、Bearer Token 等方法以确保安全连接。

</McpClient>

<McpClient
  name="Apify MCP Tester"
  homepage="https://github.com/apify/tester-mcp-client"
  supports="Tools, Discovery"
>

Apify MCP Tester 是一个开源客户端，使用服务器发送事件 (SSE) 连接到任何 MCP 服务器。
它是一个独立的 Apify Actor，专为通过 SSE 测试 MCP 服务器而设计，支持 Authorization 头。
它使用纯 JavaScript（老式风格）并托管在 Apify 上，允许您在无需任何设置的情况下运行它。

**主要功能：**

- 通过 SSE 连接到任何 MCP 服务器。
- 与 [Apify MCP 服务器](https://mcp.apify.com) 配合使用，与一个或多个 Apify [Actors](https://apify.com/store) 交互。
- 根据上下文和用户查询动态利用工具（如果服务器支持）。

</McpClient>

<McpClient
  name="Apigene MCP Client"
  homepage="https://apigene.ai"
  supports="Tools, Resources, Discovery"
  instructions="https://docs.apigene.ai/user-guide/copilot"
>

Apigene MCP 客户端是一个 AI 驱动的对话界面，通过自然语言实现与多个应用程序、API 和 MCP 服务器的无缝交互。它提供了一个统一的界面，用于在不同的 AI 平台上部署代理，具有优化的性能和治理。

**主要功能：**

- **多 LLM 兼容性**：与所有领先的 AI 平台无缝协作，包括 Claude、OpenAI (ChatGPT)、Gemini、xAI 和 OpenRouter。无需修改即可在不同平台上部署相同的代理。
- **针对成本和性能优化**：动态工具加载仅在需要时加载工具， enabling 数千个工具而不会导致上下文膨胀。工具输出优化通过紧凑的 JSON 表示提供高达 99% 的有效负载减少。并行执行同时运行多个工具调用，响应速度提高 10 倍。
- **统一的多工具界面**：将多个 API 和 MCP 服务器 mesh 到一个代理中。从一个 Copilot 界面无缝地与所有工具交互，无需胶水代码或特定于框架的逻辑。
- **受控访问和审计**：细粒度访问控制定义每个用户或代理可以执行的确切操作。完整的审计跟踪记录每个工具调用的时间戳、输入和输出以符合合规性。

**了解更多：**

- [Apigene Copilot 文档](https://docs.apigene.ai/user-guide/copilot)

</McpClient>

<McpClient
  name="Archestra"
  homepage="https://archestra.ai"
  supports="Tools, Apps, CIMD, DCR, Enterprise-Managed Authorization"
>

Archestra 是一个企业级 AI 平台，将 LLM 代理、MCP 注册表/编排器、MCP 网关、代理运行时和聊天 UI 结合到一个单一的控制平面中，用于构建、路由和保护 AI 工作流。

**主要功能：**

- 统一的 MCP 网关，暴露单个端点用于编排远程和自托管 MCP 服务器上的工具。
- 支持 MCP Apps，用于聊天中的内联交互式工具 UI。
- 支持 DCR 和 CIMD 用于 MCP 原生 OAuth 2.1 客户端注册。
- 支持企业托管授权扩展，用于集中管理的企业身份流程。
- 包括具有确定性、上下文感知工具护栏的 LLM 代理，以减少提示注入和数据泄露风险。
- 添加每团队成本跟踪、使用限制和模型流量优化控制。

</McpClient>

<McpClient
  name="Augment Code"
  homepage="https://augmentcode.com"
  supports="Tools"
  instructions="https://docs.augmentcode.com/setup-augment/mcp"
>

Augment Code 是一个用于 VS Code 和 JetBrains 的 AI 驱动编码平台，具有自主代理、聊天和补全功能。本地和远程代理都 backed by 完整的代码库感知和对 MCP 的原生支持，通过外部来源和工具增强上下文。

**主要功能：**

- 本地和远程代理中的完整 MCP 支持。
- 通过 MCP 服务器添加额外上下文。
- 使用 MCP 工具自动化您的开发工作流。
- 适用于 VS Code 和 JetBrains IDEs。

</McpClient>

<McpClient
  name="Avatar Shell"
  homepage="https://github.com/mfukushim/avatar-shell"
  supports="Resources, Tools"
>

Avatar-Shell 是一个基于 electron 的 MCP 客户端应用程序，优先考虑头像对话和媒体输出（如图像）。

**主要功能：**

- 可以使用 MCP 工具和资源
- 支持通过 socket.io 进行头像到头像的通信。
- 支持混合使用多个 LLM API。
- 守护进程机制允许灵活的调度。

</McpClient>

<McpClient
  name="BeeAI Framework"
  homepage="https://framework.beeai.dev"
  sourceCode="https://github.com/i-am-bee/beeai-framework"
  supports="Tools"
>

BeeAI Framework 是一个开源框架，用于大规模构建、部署和服务强大的代理工作流。该框架包括 **MCP Tool**，这是一个原生功能，简化了将 MCP 服务器集成到代理工作流中。

**主要功能：**

- 无缝地将 MCP 工具纳入代理工作流。
- 从连接的 MCP 客户端快速实例化框架原生工具。
- 计划未来支持代理 MCP 功能。

**了解更多：**

- [在代理工作流中使用 MCP 工具的示例](https://i-am-bee.github.io/beeai-framework/#/typescript/tools?id=using-the-mcptool-class)

</McpClient>

<McpClient
  name="BoltAI"
  homepage="https://boltai.com"
  supports="Tools"
>

BoltAI 是一个原生、多合一的 AI 聊天客户端，支持 MCP。BoltAI 支持多个 AI 提供商（OpenAI、Anthropic、Google AI...），包括本地 AI 模型（通过 Ollama、LM Studio 或 LMX）

**主要功能：**

- MCP 工具集成：配置后，用户可以在每个聊天中启用单个 MCP 服务器
- MCP 快速设置：从 Claude Desktop 应用程序或 Cursor 编辑器导入配置
- 在任何应用程序中使用 AI 命令功能调用 MCP 工具
- 在移动应用程序中集成远程 MCP 服务器

**了解更多：**

- [BoltAI 文档](https://boltai.com/docs/plugins/mcp-servers)
- [BoltAI 网站](https://boltai.com)

</McpClient>

<McpClient
  name="Call Chirp"
  homepage="https://www.call-chirp.com"
  supports="Prompts, Tools"
>

Call Chirp 使用 AI 捕获业务对话中的每个关键细节，自动将见解同步到您的 CRM 和项目工具中，这样您就不会再错过任何达成交易的时刻。

**主要功能：**

- 保存来自 Zoom、Google Meet 等的转录
- 用于语音 AI 代理的 MCP 工具
- 支持远程 MCP 服务器

</McpClient>

<McpClient
  name="Chatbox"
  homepage="https://chatboxai.app"
  sourceCode="https://github.com/chatboxai/chatbox"
  supports="Tools"
  instructions="https://docs.chatboxai.app/guides/mcp"
>

Chatbox 是一个更好的 UI 和桌面应用程序，适用于 ChatGPT、Claude 和其他 LLM，可在 Windows、Mac、Linux 和 Web 上使用。它是开源的，并在 GitHub 上获得了 37K 星。

**主要功能：**

- 支持 MCP 服务器的工具
- 支持本地和远程 MCP 服务器
- 内置 MCP 服务器市场

</McpClient>

<McpClient
  name="ChatFrame"
  homepage="https://chatframe.co"
  supports="Tools"
>

ChatFrame 是一个跨平台桌面聊天机器人，统一访问多个 AI 语言模型，支持通过 MCP 服务器自定义工具集成，并支持与本地文件进行 RAG 对话—— all in a single, polished app for macOS and Windows.

**主要功能：**

- 在一个界面中统一访问顶级 LLM 提供商（OpenAI、Anthropic、DeepSeek、xAI 等）
- 内置检索增强生成 (RAG)，用于在您的 PDF、文本和代码文件中进行即时、私密的搜索
- 通过模型上下文协议 (MCP) 服务器自定义工具的插件系统
- 多模态聊天：支持图像、文本和实时交互式工件

</McpClient>

<McpClient
  name="ChatGPT"
  homepage="https://chatgpt.com"
  supports="Tools, Apps, DCR"
  instructions="https://platform.openai.com/docs/guides/developer-mode"
>

ChatGPT 是 OpenAI 的 AI 助手，为远程服务器提供 MCP 支持以进行深度研究并为基于 MCP 的应用程序提供支持。

**主要功能：**

- 通过设置中的连接 UI 支持 MCP
- 访问配置的 MCP 服务器中的搜索工具以进行深度研究
- 支持 MCP Apps，允许 ChatGPT 连接到基于 MCP 的应用程序
- 企业级安全和合规功能

</McpClient>

<McpClient
  name="ChatWise"
  homepage="https://chatwise.app"
  supports="Tools"
>

ChatWise 是一个桌面优化、高性能的聊天应用程序，让您可以使用自己的 API 密钥。它支持广泛的 LLM 并与 MCP 集成以启用工具工作流。

**主要功能：**

- 支持 MCP 服务器的工具
- 提供内置工具，如 Web 搜索、工件和图像生成。

</McpClient>

<McpClient
  name="Chorus"
  homepage="https://chorus.sh"
  supports="Tools"
>

Chorus 是一个用于与 AI 聊天的原生 Mac 应用程序。同时与多个模型聊天，运行工具和 MCP，创建项目，快速聊天，使用自己的密钥， all in a blazing fast, keyboard shortcut friendly app.

**主要功能：**

- 一键安装支持 MCP
- 内置工具，如 Web 搜索、终端和图像生成
- 同时与多个模型聊天（云或本地）
- 创建具有范围记忆的项目
- 与可以看到您屏幕的 AI 快速聊天

</McpClient>

<McpClient
  name="Claude Code"
  homepage="https://claude.com/product/claude-code"
  supports="Resources, Prompts, Tools, Roots, Elicitation, Instructions, Discovery, DCR"
  instructions="https://code.claude.com/docs/en/mcp"
>

Claude Code 是 Anthropic 的交互式代理编码工具，通过自然语言命令帮助您更快地编码。它支持资源、提示词、工具和 roots 的 MCP 集成，并且还可以作为 MCP 服务器与其他客户端集成。

**主要功能：**

- 全面支持来自 MCP 服务器的资源、提示词、工具和 roots
- 通过 MCP 服务器提供自己的工具，以便与其他 MCP 客户端集成

</McpClient>

<McpClient
  name="Claude Desktop App"
  homepage="https://claude.ai/download"
  supports="Resources, Prompts, Tools, Roots, Apps, DCR"
  instructions={[
    ["本地服务器", "https://support.claude.com/en/articles/10949351-getting-started-with-local-mcp-servers-on-claude-desktop"],
    ["远程服务器", "https://support.claude.com/en/articles/11175166-getting-started-with-custom-connectors-using-remote-mcp"]
  ]}
>

Claude Desktop 提供对 MCP 的全面支持，实现与本地工具和数据源的深度集成。

**主要功能：**

- 全面支持资源，允许附加本地文件和数据
- 支持提示词模板
- 工具集成用于执行命令和脚本
- 本地服务器连接以增强隐私和安全性

</McpClient>

<McpClient
  name="Claude.ai"
  homepage="https://claude.ai"
  supports="Resources, Prompts, Tools, Apps, CIMD, DCR"
>

Claude.ai 是 Anthropic 的基于 Web 的 AI 助手，为远程服务器提供 MCP 支持。

**主要功能：**

- 通过设置中的集成 UI 支持远程 MCP 服务器
- 访问配置的 MCP 服务器中的工具、提示词和资源
- 与 Claude 的对话界面无缝集成
- 企业级安全和合规功能

</McpClient>

<McpClient
  name="Cline"
  homepage="https://github.com/cline/cline"
  supports="Resources, Tools, Discovery"
  instructions="https://docs.cline.bot/mcp/configuring-mcp-servers"
>

Cline 是 VS Code 中的自主编码代理，可以编辑文件、运行命令、使用浏览器等——每一步都需要您的许可。

**主要功能：**

- 通过自然语言创建和添加工具（例如“添加一个搜索 Web 的工具”）
- 通过 `~/Documents/Cline/MCP` 目录与他人共享 Cline 创建的自定义 MCP 服务器
- 显示配置的 MCP 服务器及其工具、资源和任何错误日志

</McpClient>

<McpClient
  name="CodeGPT"
  homepage="https://codegpt.co"
  supports="Tools"
>

CodeGPT 是一个流行的 VS Code 和 Jetbrains 扩展，将 AI 驱动的编码助手带到您的编辑器中。它支持与 MCP 服务器集成工具，允许用户在开发工作流中直接利用外部 AI 功能。

**主要功能：**

- 使用任何配置的 MCP 服务器中的 MCP 工具
- 与 VS Code 和 Jetbrains UI 无缝集成
- 支持多个 LLM 提供商和自定义端点

**了解更多：**

- [CodeGPT 文档](https://docs.codegpt.co/)

</McpClient>

<McpClient
  name="Codex"
  homepage="https://github.com/openai/codex"
  supports="Resources, Tools, Elicitation"
  instructions="https://developers.openai.com/codex/mcp/"
>

Codex 是 OpenAI 的一个轻量级 AI 驱动编码代理，在您的终端中运行。

**主要功能：**

- 支持 MCP 工具（列表和调用）
- 支持 MCP 资源（列表、读取和模板）
- 支持 Elicitation（将请求路由到 TUI 以获取用户输入）
- 支持带有 OAuth 的 STDIO 和 HTTP 流传输
- 也可作为 VS Code 扩展使用

</McpClient>

<McpClient
  name="Continue"
  homepage="https://github.com/continuedev/continue"
  supports="Resources, Prompts, Tools, Apps"
  instructions="https://docs.continue.dev/customize/deep-dives/mcp"
>

Continue 是一个开源 AI 代码助手，内置支持 MCP 工具、资源、提示词和 Apps

**主要功能：**

- 输入 "@" 提及 MCP 资源
- 提示词模板显示为斜杠命令
- 直接在聊天中使用内置和 MCP 工具
- 有限的 MCP Apps 支持用于显示 MCP UI
- 支持 VS Code 和 JetBrains IDEs，以及任何 LLM

</McpClient>

<McpClient
  name="Copilot-MCP"
  homepage="https://github.com/VikashLoomba/copilot-mcp"
  supports="Resources, Tools"
>

Copilot-MCP 通过 MCP 启用 AI 编码助手。

**主要功能：**

- 支持 MCP 工具和资源
- 与开发工作流集成
- 可扩展的 AI 功能

</McpClient>

<McpClient
  name="Cursor"
  homepage="https://docs.cursor.com/context/mcp#protocol-support"
  supports="Prompts, Tools, Roots, Elicitation, DCR"
  instructions="https://docs.cursor.com/context/model-context-protocol"
>

Cursor 是一个 AI 代码编辑器。

**主要功能：**

- 支持 Cursor Composer 中的 MCP 工具
- 支持 roots
- 支持提示词
- 支持 elicitation
- 支持 STDIO 和 SSE

</McpClient>

<McpClient
  name="Daydreams"
  homepage="https://github.com/daydreamsai/daydreams"
  supports="Resources, Prompts, Tools"
>

Daydreams 是一个用于在链上执行任何操作的生成代理框架

**主要功能：**

- 在配置中支持 MCP 服务器
- 暴露 MCP 客户端

</McpClient>

<McpClient
  name="ECA - Editor Code Assistant"
  homepage="https://eca.dev"
  sourceCode="https://github.com/editor-code-assistant/eca"
  supports="Resources, Prompts, Tools, Roots"
>

ECA 是一个免费且开源的编辑器无关工具，旨在轻松链接 LLM 和编辑器，使用定义良好的协议为 AI 结对编程提供最佳 UX

**主要功能：**

- **编辑器无关**：任何编辑器集成的协议。
- **单一配置**：配置 eca 使其通过全局或本地配置在任何编辑器中同样工作。
- **聊天** 界面：提问、审查代码、共同编码。
- **代理**：让 LLM 作为代理工作，使用其原生工具和您可以配置的 MCP。
- **上下文** 支持：向 LLM 提供有关您的代码的更多详细信息，包括 MCP 资源和提示词。
- **多模型**：登录 OpenAI、Anthropic、Copilot、Ollama 本地模型等。
- **OpenTelemetry**：导出工具、提示词、服务器使用情况的指标。

</McpClient>

<McpClient
  name="Emacs Mcp"
  homepage="https://github.com/lizqwerscott/mcp.el"
  supports="Tools"
>

Emacs Mcp 是一个旨在与 MCP 服务器接口的 Emacs 客户端，实现无缝连接和交互。它为 [gptel](https://github.com/karthink/gptel) 和 [llm](https://github.com/ahyatt/llm) 等 AI 插件提供 MCP 工具调用支持，遵循 Emacs 的标准工具调用格式。此集成增强了 Emacs 生态系统中 AI 工具的功能。

**主要功能：**

- 为 Emacs 提供 MCP 工具支持。

</McpClient>

<McpClient
  name="fast-agent"
  homepage="https://github.com/evalstate/fast-agent"
  supports="Resources, Prompts, Tools, Discovery, Sampling, Roots, Elicitation, Instructions"
>

fast-agent 是一个 Python 代理框架，具有用于创建代理和工作流的简单声明性支持，并对 Anthropic 和 OpenAI 模型提供完整的多模态支持。

**主要功能：**

- 基于 MCP 原生类型的 PDF 和图像支持
- 用于开发和诊断代理应用程序的交互式前端，包括直通和播放模拟器
- 内置支持“构建有效代理”工作流。
- 将代理部署为 MCP 服务器

</McpClient>

<McpClient
  name="Firebender"
  homepage="https://firebender.com"
  supports="Tools"
  instructions="https://docs.firebender.com/context/mcp"
>

Firebender 是一个 IntelliJ 插件，提供世界级的编码代理，具有用于工具调用的 MCP 集成。

**主要功能：**

- 工具集成用于通过 STDIO 执行命令和脚本，通过 mcp-remote npm 包间接支持 SSE。
- 本地服务器连接以增强隐私和安全性
- 可以通过项目规则或本地工作站规则文件安装 MCP。
- 可以关闭 MCP 中的单个工具。

</McpClient>

<McpClient
  name="FlowDown"
  homepage="https://github.com/Lakr233/FlowDown"
  supports="Tools"
>

FlowDown 是一个用于使用 AI/LLM 的极速流畅客户端应用程序，强调隐私和用户体验。它支持 MCP 服务器以通过外部工具扩展其功能，允许用户构建强大的自定义工作流。

**主要功能：**

- **无缝 MCP 集成**：轻松连接到 MCP 服务器以利用各种外部工具。
- **隐私优先设计**：您的数据保留在您的设备上。我们不收集任何用户数据，确保完全隐私。
- **轻量级且高效**：紧凑且优化的设计确保与任何 AI 模型都能获得流畅且响应迅速的体验。
- **广泛的兼容性**：适用于所有兼容 OpenAI 的服务提供商，并通过 MLX 支持本地离线模型。
- **丰富的用户体验**：具有格式精美的 Markdown、极速文本渲染和智能自动聊天标题。

**了解更多：**

- [FlowDown 网站](https://flowdown.ai/)
- [FlowDown 文档](https://apps.qaq.wiki/docs/flowdown/)

</McpClient>

<McpClient
  name="FLUJO"
  homepage="https://github.com/mario-andreschak/flujo"
  supports="Tools"
>

Think n8n + ChatGPT。FLUJO 是一个桌面应用程序，与 MCP 集成，为 AI 交互提供工作流构建器界面。使用 Next.js 和 React 构建，支持在线和离线 (ollama) 模型，集中管理 API 密钥和环境变量，并可以从 GitHub 安装 MCP 服务器。FLUJO 有一个 ChatCompletions 端点，流程可以从其他 AI 应用程序（如 Cline、Roo 或 Claude）执行。

**主要功能：**

- 环境和 API 密钥管理
- 模型管理
- MCP 服务器集成
- 工作流编排
- 聊天界面

</McpClient>

<McpClient
  name="Gemini CLI"
  homepage="https://github.com/google-gemini/gemini-cli"
  supports="Prompts, Tools, Instructions, DCR"
  instructions="https://geminicli.com/docs/tools/mcp-server/"
>

Gemini CLI 是一个开源 AI 代理，将 Gemini 的力量直接带入您的终端。

</McpClient>

<McpClient
  name="GenAIScript"
  homepage="https://microsoft.github.io/genaiscript/"
  sourceCode="https://github.com/microsoft/genaiscript"
  supports="Resources, Tools"
>

使用 GenAIScript（在 JavaScript 中）以编程方式组装 LLM 提示词。在 JavaScript 中编排 LLM、工具和数据。

**主要功能：**

- 用于处理提示词的 JavaScript 工具箱
- 抽象化使其易于且富有成效
- 无缝 Visual Studio Code 集成

</McpClient>

<McpClient
  name="Genkit"
  homepage="https://github.com/firebase/genkit"
  supports="Resources (partial), Prompts, Tools"
>

Genkit 是一个跨语言 SDK，用于在应用程序中构建和集成 GenAI 功能。[genkitx-mcp](https://github.com/firebase/genkit/tree/main/js/plugins/mcp) 插件支持将 MCP 服务器作为客户端使用，或从 Genkit 工具和提示词创建 MCP 服务器。

**主要功能：**

- 支持工具和提示词的客户端（资源部分支持）
- 在 Genkit 的 Dev UI playground 中支持丰富的发现
- 与 Genkit 现有工具和提示词的无缝互操作性
- 适用于来自顶级提供商的各种 GenAI 模型

</McpClient>

<McpClient
  name="GitHub Copilot coding agent"
  homepage="https://docs.github.com/en/copilot/concepts/about-copilot-coding-agent"
  supports="Tools, DCR"
>

将任务委托给 GitHub Copilot 编码代理，让它后台工作，而您专注于最高影响和最有趣的工作

**主要功能：**

- 从 GitHub Issues、Visual Studio Code、GitHub Copilot Chat 或使用 GitHub MCP 服务器的您喜欢的 MCP 主机将任务委托给 Copilot
- 通过 [自定义代理的开发环境](https://docs.github.com/en/enterprise-cloud@latest/copilot/how-tos/agents/copilot-coding-agent/customizing-the-development-environment-for-copilot-coding-agent#preinstalling-tools-or-dependencies-in-copilots-environment) 或 [编写自定义指令](https://docs.github.com/en/enterprise-cloud@latest/copilot/how-tos/agents/copilot-coding-agent/best-practices-for-using-copilot-to-work-on-tasks#adding-custom-instructions-to-your-repository)  tailored Copilot 到您的项目
- [使用 MCP 工具增强 Copilot 的上下文和功能](https://docs.github.com/en/enterprise-cloud@latest/copilot/how-tos/agents/copilot-coding-agent/extending-copilot-coding-agent-with-mcp)，支持本地和远程 MCP 服务器

</McpClient>

<McpClient
  name="Glama"
  homepage="https://glama.ai/chat"
  supports="Discovery, Elicitation, Instructions, Prompts, Resources, Sampling, Tasks, Tools"
>

Glama 是一个全面的 AI 工作区和集成平台，提供统一界面访问领先的 LLM 提供商，包括 OpenAI、Anthropic 等。它支持模型上下文协议 (MCP) 生态系统，使开发者和企业能够轻松发现、构建和管理 MCP 服务器。

**主要功能：**

- 集成 [MCP 服务器目录](https://glama.ai/mcp/servers)
- 集成 [MCP 工具目录](https://glama.ai/mcp/tools)
- 托管 MCP 服务器并通过聊天或 SSE 端点访问它们
  – 能够同时与多个 LLM 和 MCP 服务器聊天
- 上传和分析本地文件和数据
- 在所有聊天和数据中进行全文搜索

</McpClient>

<McpClient
  name="goose"
  homepage="https://github.com/block/goose"
  supports="Apps, DCR, Discovery, Elicitation, Instructions, Prompts, Resources, Roots, Sampling, Tools"
  instructions="https://block.github.io/goose/docs/getting-started/using-extensions/"
>

goose 是一个开源 AI 代理，通过自动化编码任务增强您的软件开发。

**主要功能：**

- 通过工具向 goose 暴露 MCP 功能。
- 可以直接通过 [扩展目录](https://block.github.io/goose/v1/extensions/)、CLI 或 UI 安装 MCP。
- goose 允许您通过 [构建自己的 MCP 服务器](https://block.github.io/goose/docs/tutorials/custom-extensions) 扩展其功能。
- 包括用于开发、记忆、计算机控制和自动可视化的内置扩展。

</McpClient>

<McpClient
  name="gptme"
  homepage="https://github.com/gptme/gptme"
  supports="Tools"
>

gptme 是一个开源的基于终端的个人 AI 助手/代理，旨在协助编程任务和一般知识工作。

**主要功能：**

- 以 CLI 为先的设计，注重简单易用
- 丰富的内置工具集，用于 shell 命令、Python 执行、文件操作和 Web 浏览
- 本地优先的方法，支持多个 LLM 提供商
- 开源，旨在可扩展且易于修改

</McpClient>

<McpClient
  name="HyperAgent"
  homepage="https://github.com/hyperbrowserai/HyperAgent"
  supports="Tools"
  instructions="https://hyperbrowser.ai/docs/hyperagent/mcp"
>

HyperAgent 是 Playwright 的 AI 增强版。使用 HyperAgent，您不再需要脆弱的脚本，只需强大的自然语言命令。使用 MCP 服务器，您可以扩展 HyperAgent 的功能，而无需编写任何代码。

**主要功能：**

- AI 命令：简单的 API，如 page.ai()、page.extract() 和 executeTask() 用于任何 AI 自动化
- 回退到常规 Playwright：当不需要 AI 时使用常规 Playwright
- 隐身模式 – 通过内置的防机器人补丁避免检测
- 云就绪 – 通过 [Hyperbrowser](https://www.hyperbrowser.ai/)  instant 扩展到数百个会话
- MCP 客户端 – 连接到 Composio 等工具以进行完整工作流（例如将 Web 数据写入 Google Sheets）

</McpClient>
<McpClient
  name="IBM Bob"
  homepage="https://bob.ibm.com"
  supports="Resources, Tools"
  instructions="https://bob.ibm.com/docs/ide/configuration/mcp/mcp-in-bob"
>

IBM Bob 是一个 AI SDLC 合作伙伴，通过 MCP 启用 AI 编码助手。Bob  built with 安全优先原则和企业级部署灵活性，通过左移实践将安全集成到开发工作流中，帮助加速现代化同时保持治理和合规性。

**主要功能：**

- 支持具有细粒度控制的 MCP 工具和资源
- 全局和项目级 MCP 服务器配置
- 支持本地和远程服务器的 STDIO 和 SSE 传输
- 单独工具启用/禁用以优化上下文使用
- 受信任工具的自动批准功能
- 通过自然语言内置 MCP 服务器创建
- 具有左移集成的企业级安全
- 与开发工作流集成

</McpClient>

<McpClient
  name="Inspector"
  homepage="https://tryinspector.com"
  supports="Tools, Prompts, Resources, DCR"
  instructions="https://tryinspector.com/docs"
>

Inspector 是一个用于代码库的可视化编辑器。它连接到 Cursor、Claude Code 和 Codex，以便您可以可视化地编辑前端。移动元素、更改文本并发布真实代码，而无需触碰 CSS。

**主要功能：**

- 设计模式：移动元素、编辑文本并放大以像 Figma 一样与前端交互。
- 代理连接：插入 Cursor、Claude Code 或 Codex。
- 版本控制：从 Inspector 暂存更改并打开 PR。
- MCP 客户端：连接任何您想要的 MCP 服务器！

</McpClient>

<McpClient
  name="Jenova"
  homepage="https://jenova.ai"
  supports="Tools, Discovery"
>

Jenova 是非技术用户的最佳 MCP 客户端，尤其是在移动端。

**主要功能：**

- 30+ 预集成 MCP 服务器，一键集成自定义服务器
- MCP 推荐功能，为特定任务建议最佳服务器
- 多代理架构，具有领先的工具使用可靠性和可扩展性，通过 RAG 驱动的服务器元数据支持无限并发 MCP 服务器连接
- 模型无关平台，支持任何领先的 LLM（OpenAI、Anthropic、Google 等）
- 无限聊天历史和由 RAG 驱动的全局持久记忆
- 轻松创建具有自定义模型、指令、知识库和 MCP 服务器的自定义代理
- 本地 MCP 服务器 (STDIO) 支持即将通过桌面应用程序推出

</McpClient>

<McpClient
  name="JetBrains AI Assistant"
  homepage="https://plugins.jetbrains.com/plugin/22282-jetbrains-ai-assistant"
  supports="Tools"
  instructions="https://www.jetbrains.com/help/ai-assistant/mcp.html"
>

JetBrains AI Assistant 插件为所有 JetBrains IDE 中可用的软件开发提供 AI 驱动的功能。

**主要功能：**

- 由 Mellum（JetBrains 的专有 AI 模型）驱动的无限代码补全。
- 上下文感知的 AI 聊天，理解您的代码并实时帮助您。
- 访问来自 OpenAI、Anthropic 和 Google 的顶级模型。
- 通过 Ollama 或 LM Studio 连接本地 LLM 的离线模式。
- 深度集成到 IDE 工作流中，包括编辑器中的代码建议、VCS 协助、运行时错误解释等。

</McpClient>

<McpClient
  name="JetBrains Junie"
  homepage="https://www.jetbrains.com/junie"
  supports="Tools"
  instructions="https://www.jetbrains.com/help/junie/model-context-protocol-mcp.html"
>

Junie 是 JetBrains 用于 JetBrains IDEs 和 Android Studio 的 AI 编码代理。

**主要功能：**

- 通过 **stdio** 连接到 MCP 服务器以使用外部工具和数据源。
- 每命令批准，带有可选的允许列表。
- 通过 `mcp.json` 配置（全局 `~/.junie/mcp.json` 或项目 `.junie/mcp/`）。

</McpClient>

<McpClient
  name="Joey"
  homepage="https://benkaiser.github.io/joey-mcp-client/"
  sourceCode="https://github.com/benkaiser/joey-mcp-client"
  supports="Prompts, Tools, Sampling, Elicitation, Apps"
>

Joey 是一个面向移动端的 MCP 客户端，适用于 **iOS 和 Android**（也可在 macOS、Windows 和 Linux 上使用），通过 OpenRouter 和远程 MCP 服务器 over Streamable HTTP 连接到 AI 模型。

**主要功能：**

- **移动 MCP 支持** — 直接在 iOS 和 Android 手机或平板电脑上使用 MCP 服务器。
- 通过 **Streamable HTTP** 连接到远程 MCP 服务器，支持 OAuth。
- 支持每次对话多个 MCP 服务器，带有工具调用。
- 支持 MCP 采样和 elicitation 用于交互式服务器发起的工作流。
- 带有 SSE 流响应的图像和音频附件。

</McpClient>

<McpClient
  name="Kilo Code"
  homepage="https://github.com/Kilo-Org/kilocode"
  supports="Resources, Tools, Discovery"
  instructions="https://kilo.ai/docs/features/mcp/using-mcp-in-kilo-code"
>

Kilo Code 是 VS Code 中的自主编码 AI 开发团队，可以编辑文件、运行命令、使用浏览器等。

**主要功能：**

- 通过自然语言创建和添加工具（例如“添加一个搜索 Web 的工具”）
- 通过 MCP 市场发现 MCP 服务器
- 通过 MCP 市场一键安装 MCP 服务器
- 显示配置的 MCP 服务器及其工具、资源和任何错误日志

</McpClient>

<McpClient
  name="Klavis AI Slack/Discord/Web"
  homepage="https://www.klavis.ai/"
  sourceCode="https://github.com/Klavis-AI/klavis"
  supports="Resources, Tools"
>

Klavis AI 是一个开源基础设施，用于轻松使用、构建和扩展 MCP。

**主要功能：**

- 用于直接使用 MCP 的 Slack/Discord/Web MCP 客户端
- 用于轻松 MCP 配置的简单 Web UI 仪表板
- 与 Slack & Discord 客户端和 MCP 服务器直接 OAuth 集成，用于安全用户身份验证
- 支持 SSE 传输

**了解更多：**

- [展示 Slack/Discord 中 MCP 用法的演示视频](https://youtu.be/9-QQAhrQWw8)

</McpClient>

<McpClient
  name="Langdock"
  homepage="https://langdock.com"
  supports="Tools"
  instructions="https://docs.langdock.com/resources/integrations/mcp"
>

Langdock 是一个企业级解决方案，用于向所有员工推出 AI，同时使开发人员能够在其上构建和部署自定义 AI 工作流。

**主要功能：**

- 远程 MCP 服务器（SSE & Streamable HTTP）支持，通过 OAuth、API 密钥或 without authentication 连接到任何 MCP 服务器。
- MCP 工具发现和管理，包括工具确认 UI。
- 企业级安全和合规功能

</McpClient>

<McpClient
  name="Langflow"
  homepage="https://github.com/langflow-ai/langflow"
  supports="Tools"
  instructions="https://docs.langflow.org/mcp-client"
>

Langflow 是一个开源可视化构建器，让开发人员能够快速原型化和构建 AI 应用程序，它作为 MCP 服务器和 MCP 客户端与模型上下文协议 (MCP) 集成。

**主要功能：**

- 全面支持使用 MCP 服务器工具构建代理和工作流。
- 将代理和工作流导出为 MCP 服务器
- 本地和远程服务器连接以增强隐私和安全性

**了解更多：**

- [展示如何将 Langflow 用作 MCP 客户端和服务器的演示视频](https://www.youtube.com/watch?v=pEjsaVVPjdI)

</McpClient>

<McpClient
  name="LibreChat"
  homepage="https://github.com/danny-avila/LibreChat"
  supports="Tools, Instructions, DCR"
  instructions="https://www.librechat.ai/docs/features/mcp"
>

LibreChat 是一个开源、可定制的 AI 聊天 UI，支持多个 AI 提供商，现在包括 MCP 集成。

**主要功能：**

- 通过 MCP 服务器扩展当前工具生态系统，包括 [代码解释器](https://www.librechat.ai/docs/features/code_interpreter) 和图像生成工具
- 使用来自顶级提供商的各种 LLM 将工具添加到可定制的 [代理](https://www.librechat.ai/docs/features/agents)
- 开源且可自托管，具有安全的多用户支持
- 未来路线图包括扩展的 MCP 功能支持

</McpClient>

<McpClient
  name="LM Studio"
  homepage="https://lmstudio.ai"
  supports="Tools"
  instructions="https://lmstudio.ai/docs/app/mcp"
>

LM Studio 是一个跨平台桌面应用程序，用于本地发现、下载和运行开源 LLM。您现在可以通过模型上下文协议 (MCP) 将本地模型连接到工具。

**主要功能：**

- 在您的计算机上将 MCP 服务器与本地模型一起使用。将条目添加到 `mcp.json` 并保存以开始。
- 工具确认 UI：当模型调用工具时，您可以在 LM Studio 应用程序中确认调用。
- 跨平台：运行于 macOS、Windows 和 Linux，一键安装程序，无需在命令行中摆弄
- 支持带有 GPU 加速的 GGUF (llama.cpp) 或 MLX 模型
- GUI 和终端模式：使用 LM Studio 应用程序或 CLI (lms) 进行脚本编写和自动化

**了解更多：**

- [文档：在 LM Studio 中使用 MCP](https://lmstudio.ai/docs/app/plugins/mcp)
- [为您的服务器创建“添加到 LM Studio"按钮](https://lmstudio.ai/docs/app/plugins/mcp/deeplink)
- [公告博客：LM Studio + MCP](https://lmstudio.ai/blog/mcp)

</McpClient>

<McpClient
  name="LM-Kit.NET"
  homepage="https://lm-kit.com/products/lm-kit-net/"
  supports="Tools"
>

LM-Kit.NET 是一个本地优先的 .NET (C# / VB.NET) 生成式 AI SDK，可以作为 **MCP 客户端**。当前 MCP 支持：**仅限工具**。

**主要功能：**

- 通过 HTTP/JSON-RPC 2.0 使用 MCP 服务器工具（初始化、列出工具、调用工具）。
- 通过 `McpClient`  programmatically 工具发现和调用。
- 易于集成到 .NET 代理和应用程序中。

**了解更多：**

- [文档：在 LM-Kit.NET 中使用 MCP](https://docs.lm-kit.com/lm-kit-net/api/LMKit.Mcp.Client.McpClient.html)
- [创建 AI 代理](https://lm-kit.com/solutions/ai-agents)
- 产品页面：[LM-Kit.NET](https://lm-kit.com/products/lm-kit-net/)

</McpClient>

<McpClient
  name="Lutra"
  homepage="https://lutra.ai"
  supports="Resources, Prompts, Tools"
>

Lutra 是一个 AI 代理，将对话转换为可操作的自动化工作流。

**主要功能：**

- 简单的 MCP 集成：将 Lutra 连接到 MCP 服务器就像提供服务器 URL 一样简单；Lutra 在后台处理其余部分。
- 聊天以采取行动：Lutra 理解您的对话上下文和目标，自动与您的现有应用程序集成以执行任务。
- 可重用的剧本：完成任务后，将步骤保存为可重用的自动化工作流——简化可重复的流程并减少手动工作。
- 可共享的自动化：轻松与团队成员共享您保存的剧本，以标准化最佳实践并加速协作工作流。

**了解更多：**

- [Lutra AI 代理解释（视频）](https://www.youtube.com/watch?v=W5ZpN0cMY70)

</McpClient>

<McpClient
  name="MCP Bundler for MacOS"
  homepage="https://mcp-bundler.maketry.xyz"
  supports="Resources, Prompts, Tools"
>

MCP Bundler 是您的 MCP 工作流的完美本地代理。该应用程序集中所有您的 MCP 服务器——瞬间切换、分组、关闭功能。在 MCP Bundler 内瞬间切换捆绑包。

**主要功能：**

- 统一控制面板：从一个清晰的 macOS 窗口管理所有您的 MCP 服务器——本地 STDIO 和远程 HTTP/SSE。瞬间启动、停止或编辑它们，无需触碰配置。
- 一键，全部连接：通过一个切换启动或禁用整个 MCP 设置。按项目或工作区切换捆绑包，并保持您的 AI 工具自动同步。
- 每工具控制：启用或隐藏每个服务器内的单个工具。保持您的捆绑包干净、轻量级，并为每个 AI 工作流量身定制。
- 即时健康和日志：实时健康指标和请求日志准确显示正在运行的内容。无需离开应用程序即可诊断和修复连接问题。
- 自动生成 MCP 配置：在几秒钟内为任何客户端复制现成的 JSON 片段。无需手动接线——将您的 Bundler 连接为单个 MCP 端点。

**了解更多：**

- [MCP Bundler 实战（视频）](https://www.youtube.com/watch?v=CEHVSShw_NU)

</McpClient>

<McpClient
  name="MCPBundles"
  homepage="https://www.mcpbundles.com/studio"
  supports="Resources, Prompts, Tools, Discovery"
>

MCPBundles 提供 MCPBundle Studio，一个基于浏览器的 MCP 客户端，用于在远程 MCP 服务器上测试和执行 MCP 工具。

**主要功能：**

- 发现和检查可用工具，带有参数模式和描述
- 支持 OAuth 和 API 密钥身份验证以确保安全的提供商连接
- 使用基于表单和基于聊天的输入执行 MCP 工具
- 实现 Apps 用于渲染来自工具的交互式 UI 响应
- 用于远程 MCP 服务器连接的 Streamable HTTP 传输

</McpClient>

<McpClient
  name="mcp-agent"
  homepage="https://github.com/lastmile-ai/mcp-agent"
  supports="Resources, Prompts, Tools, Sampling (partial), Roots, Elicitation"
  instructions="https://docs.mcp-agent.com/reference/configuration"
>

mcp-agent 是一个简单、可组合的框架，用于使用模型上下文协议构建代理。

**主要功能：**

- 自动管理 MCP 服务器的连接。
- 向 LLM 暴露来自多个服务器的工具。
- 实现 [构建有效代理](https://www.anthropic.com/research/building-effective-agents) 中定义的每种模式。
- 支持工作流暂停/恢复信号，例如等待人类反馈。

</McpClient>

<McpClient
  name="mcp-client-chatbot"
  homepage="https://github.com/cgoinglove/mcp-client-chatbot"
  supports="Tools"
>

mcp-client-chatbot 是一个使用 Vercel 的 Next.js、AI SDK 和 Shadcn UI 构建的本地优先聊天机器人。

**主要功能：**

- 它支持标准 MCP 工具调用，并包括一个自定义 MCP 服务器和一个独立 UI，用于在聊天流之外测试 MCP 工具。
- 所有 MCP 工具默认提供给 LLM，但该项目还包括一个可选的 `@toolname` 提及功能，使工具调用更加明确——特别是在连接到具有许多工具的多个 MCP 服务器时非常有用。
- 可视化工作流构建器，让您可以通过链接 LLM 节点和 MCP 工具创建自定义工具。发布的工作流可作为聊天中的 `@workflow_name` 工具调用，实现复杂的多步自动化序列。

</McpClient>

<McpClient
  name="mcp-use"
  homepage="https://github.com/pietrozullo/mcp-use"
  supports="Resources, Prompts, Tools, Discovery, Sampling, Elicitation"
>

mcp-use 是一个开源 python 库，用于非常轻松地将任何 LLM 连接到任何本地和远程的 MCP 服务器。

**主要功能：**

- 非常简单的界面，将任何 LLM 连接到任何 MCP。
- 支持创建自定义代理、工作流。
- 支持同时连接到多个 MCP 服务器。
- 支持所有 langchain 支持的模型，也包括本地。
- 提供高效的工具编排和搜索功能。

</McpClient>

<McpClient
  name="mcpc"
  homepage="https://github.com/apify/mcpc"
  supports="Resources, Prompts, Tools, Discovery, Instructions"
>

`mcpc` 是一个用于 MCP 的通用 CLI 客户端，将 MCP 操作映射到直观的命令，用于交互式 shell 使用、脚本和 AI 编码代理。

**主要功能：**

- MCP 的瑞士军刀：支持 stdio 和 streamable HTTP、服务器配置文件和零配置、OAuth 2.1、HTTP 头以及主要 MCP 功能。
- 用于同时与多个服务器交互的持久会话。
- 结构化文本输出使 AI 代理能够探索并与 MCP 服务器交互。
- JSON 输出和模式验证允许与其他 CLI 工具、脚本和 shell 中的 MCP **代码模式** 稳定集成。
- 代理 MCP 服务器，为 AI 代码沙箱提供对经过身份验证的 MCP 会话的安全访问。

</McpClient>

<McpClient
  name="MCPHub"
  homepage="https://github.com/ravitemer/mcphub.nvim"
  supports="Resources, Prompts, Tools"
>

MCPHub 是一个强大的 Neovim 插件，将 MCP (模型上下文协议) 服务器集成到您的工作流中。

**主要功能：**

- 使用直观的 UI 安装、配置和管理 MCP 服务器。
- 内置 Neovim MCP 服务器，支持文件操作（读取、写入、搜索、替换）、命令执行、终端集成、LSP 集成、缓冲区和诊断。
- 直接在 Neovim 中创建基于 Lua 的 MCP 服务器。
- 与流行的 Neovim 聊天插件 Avante.nvim 和 CodeCompanion.nvim 集成

</McpClient>

<McpClient
  name="MCPJam"
  homepage="https://github.com/MCPJam/inspector"
  supports="Resources, Prompts, Tools, Elicitation, Instructions, Tasks, Apps, CIMD, DCR"
  instructions="https://docs.mcpjam.com/getting-started"
>

MCPJam Inspector 是用于 ChatGPT 应用、MCP 扩展应用和 MCP 服务器的本地开发客户端。

**主要功能：**

- ChatGPT Apps SDK 和 MCP 扩展应用的本地模拟器。不再需要 ChatGPT 订阅或 ngrok。
- OAuth 调试器，可视化检查 MCP 服务器 OAuth 的每一步。
- LLM  playground，可使用任何 LLM 与您的 MCP 服务器聊天。我们免费提供自己的 API 令牌。
- 连接、测试和检查任何本地或远程的 MCP 服务器。手动调用 MCP 工具、资源、提示词等。查看所有 JSON-RPC 日志。
- 支持所有传输方式 - STDIO、SSE 和 Streamable HTTP。

</McpClient>

<McpClient
  name="MCPOmni-Connect"
  homepage="https://github.com/Abiorh001/mcp_omni_connect"
  supports="Resources, Prompts, Tools, Sampling"
>

MCPOmni-Connect 是一个多功能命令行界面 (CLI) 客户端，旨在通过 stdio 和 SSE 传输连接到各种模型上下文协议 (MCP) 服务器。

**主要功能：**

- 支持资源、提示词、工具和采样
- 具有 ReAct 和编排器能力的代理模式
- 与 OpenAI 模型和其他 LLM 无缝集成
- 跨多个服务器动态管理工具和资源
- 支持 stdio 和 SSE 传输协议
- 全面的工具编排和资源分析能力

</McpClient>

<McpClient
  name="Memex"
  homepage="https://memex.tech/"
  supports="Resources, Prompts, Tools"
>

Memex 是首款 MCP 客户端和 MCP 服务器构建器——多合一桌面应用。与仅消耗现有服务器的传统 MCP 客户端不同，Memex 可以从自然语言提示词创建自定义 MCP 服务器，立即将它们集成到其工具包中，并使用它们解决问题—— all within a single conversation.

**主要功能：**

- **提示词到 MCP 服务器**：从自然语言描述生成功能齐全的 MCP 服务器
- **自测试与调试**：自动测试、调试和改进创建的 MCP 服务器
- **通用 MCP 客户端**：通过直观的自然语言集成与任何 MCP 服务器配合工作
- **精选 MCP 目录**：访问经过测试、一键安装的 MCP 服务器（Neon、Netlify、GitHub、Context7 等）
- **多服务器编排**：同时利用多个 MCP 服务器处理复杂工作流

**了解更多：**

- [Memex Launch 2: MCP 团队和代理 API](https://memex.tech/blog/memex-launch-2-mcp-teams-and-agent-api-private-preview-125f)

</McpClient>

<McpClient
  name="Memgraph Lab"
  homepage="https://memgraph.com/lab"
  supports="Resources, Prompts, Tools, Sampling, Elicitation, Instructions"
  instructions="https://memgraph.com/docs/memgraph-lab/features/graphchat#mcp-servers"
>

[Memgraph Lab](https://memgraph.com/lab) 是 Memgraph 图数据库的可视化管理工具。其 [GraphChat](https://memgraph.com/docs/memgraph-lab/features/graphchat) 功能让您可以使用自然语言查询图数据，并通过 MCP 服务器集成扩展您的 AI 工作流。

**主要功能：**

- 构建由知识图谱作为数据骨干支持的 GraphRAG 工作流
- 通过 `SSE` 或 `Streamable HTTP` 连接远程 MCP 服务器
- 支持 MCP 资源、提示词、工具、采样、诱导和指令
- 创建具有不同配置的多个代理，便于比较和调试
- 适用于各种 LLM 提供商（OpenAI、Azure OpenAI、Anthropic、Gemini、Ollama、DeepSeek）
- 可作为桌面应用或 Docker 容器使用

**了解更多：**

- [Memgraph Lab: MCP 集成](https://memgraph.com/docs/memgraph-lab/features/graphchat#mcp-servers)

</McpClient>

<McpClient
  name="Microsoft Copilot Studio"
  homepage="https://learn.microsoft.com/en-us/microsoft-copilot-studio/agent-extend-action-mcp"
  supports="Resources, Tools, Discovery"
>

Microsoft Copilot Studio 是一个强大的 SaaS 平台，专为构建自定义 AI 驱动的应用程序和智能代理而设计，使开发人员能够创建、部署和管理复杂的 AI 解决方案。

**主要功能：**

- 支持 MCP 工具
- 使用 MCP 服务器扩展 Copilot Studio 代理
- 利用 Microsoft 统一、受监管且安全的 API 管理解决方案

</McpClient>

<McpClient
  name="MindPal"
  homepage="https://mindpal.io"
  supports="Tools"
>

MindPal 是一个用于构建和运行业务流程 AI 代理和多代理工作流的无代码平台。

**主要功能：**

- 无代码构建自定义 AI 代理
- 连接任何 SSE MCP 服务器以扩展代理工具
- 为复杂业务流程创建多代理工作流
- 对技术和非技术人员都友好
- 持续开发，不断改进 MCP 支持

**了解更多：**

- [MindPal MCP 文档](https://docs.mindpal.io/agent/mcp)

</McpClient>

<McpClient
  name="Mistral AI: Le Chat"
  homepage="https://mistral.ai"
  supports="Tools"
  instructions="https://help.mistral.ai/en/articles/393572-configuring-a-custom-connector"
>

Mistral AI: Le Chat 是 Mistral AI 助手，支持远程服务器和企业工作流的 MCP。

**主要功能：**

- 远程 MCP 服务器集成
- 企业级安全
- 与结构化数据的低延迟、高吞吐量交互

**了解更多：**

- [Mistral MCP 文档](https://help.mistral.ai/en/collections/911943-connectors)

</McpClient>

<McpClient
  name="modelcontextchat.com"
  homepage="https://modelcontextchat.com"
  supports="Tools"
>

modelcontextchat.com 是一个基于网页的 MCP 客户端，专为使用远程 MCP 服务器而设计，具有全面的身份验证支持并与 OpenRouter 集成。

**主要功能：**

- 用于远程 MCP 服务器连接的基于网页的界面
- 支持基于 Header 的授权以安全访问服务器
- OAuth 身份验证集成
- 支持 OpenRouter API Key 以访问各种 LLM 提供商
- 无需安装 - 可从任何网页浏览器访问

</McpClient>

<McpClient
  name="MooPoint"
  homepage="https://moopoint.io"
  supports="Tools, Sampling"
>

MooPoint 是一个为开发者和高级用户构建的基于网页的 AI 聊天平台，让您可以通过单个统一界面与多个大型语言模型 (LLM) 交互。连接您自己的 API 密钥（OpenAI、Anthropic 等）并安全管理自定义 MCP 服务器集成。

**主要功能：**

- 可从任何 PC 或智能手机访问—无需安装
- 选择您首选的 LLM 提供商
- 支持 `SSE`、`Streamable HTTP`、`npx` 和 `uvx` MCP 服务器
- 支持 OAuth 和采样
- 每天添加新功能

</McpClient>

<McpClient
  name="Msty Studio"
  homepage="https://msty.ai"
  supports="Tools"
>

Msty Studio 是一个隐私优先的 AI 生产力平台，无缝集成本地和在线语言模型 (LLM) 到可定制的工作流中。Msty Studio 专为技术和非技术人员设计，提供一套工具来增强 AI 交互、自动化任务并保持对数据和模型行为的完全控制。

**主要功能：**

- **工具箱和工具集**：使用符合 MCP 的配置将 AI 模型连接到本地工具和脚本。将工具分组到工具集中，以便在对话中启用动态的多步工作流。
- **Turnstiles**：创建自动化的多步 AI 交互，允许复杂的数据处理和决策流。
- **实时数据集成**：通过集成实时网页搜索功能，使用最新信息增强 AI 响应。
- **拆分聊天和分支**：同时与多个模型进行并行对话，实现比较分析和多样化视角。

**了解更多：**

- [Msty Studio 文档](https://docs.msty.studio/features/toolbox/tools)

</McpClient>

<McpClient
  name="Needle"
  homepage="https://needle.app"
  supports="Resources, Prompts, Tools, Discovery"
  instructions="https://docs.needle.app/docs/guides/mcp/getting-started/"
>

Needle 是一个 RAG 工作流平台，也可作为 MCP 客户端，让您可以在几秒钟内连接和使用 MCP 服务器。

**主要功能：**

- **即时 MCP 集成：** 在几秒钟内将任何远程 MCP 服务器连接到您的集合
- **内置 RAG：** 自动获得开箱即用的检索增强生成
- **安全 OAuth：** 连接服务器时安全、基于令牌的身份验证
- **智能预览：** 查看每个 MCP 服务器的功能并有选择地启用您需要的工具

**了解更多：**

- [入门指南](https://docs.needle.app/docs/guides/hello-needle/getting-started/)

</McpClient>

<McpClient
  name="NVIDIA Agent Intelligence (AIQ) toolkit"
  homepage="https://github.com/NVIDIA/AIQToolkit"
  supports="Tools"
>

NVIDIA Agent Intelligence (AIQ) toolkit 是一个灵活、轻量且统一的库，允许您轻松地将现有企业代理连接到任何框架中的数据源和工具。

**主要功能：**

- 作为 MCP **客户端** 使用远程工具
- 作为 MCP **服务器** 暴露工具
- 框架无关，兼容 LangChain、CrewAI、Semantic Kernel 和自定义代理
- 包含内置的可观察性和评估工具

**了解更多：**

- [AIQ toolkit MCP 文档](https://docs.nvidia.com/aiqtoolkit/latest/workflows/mcp/index.html)

</McpClient>

<McpClient
  name="opencode"
  homepage="https://opencode.ai"
  sourceCode="https://github.com/anomalyco/opencode"
  supports="Resources, Prompts, Tools"
  instructions="https://opencode.ai/docs/mcp-servers/"
>

OpenCode 是一个开源 AI 编码代理。它可用作基于终端的界面、桌面应用或 IDE 扩展。

**主要功能：**

- 支持 MCP 工具
- 在 cli 中使用 `@` 前缀支持 MCP 资源
- 在 cli 中使用 `/` 前缀支持 MCP 提示词作为斜杠命令

</McpClient>

<McpClient
  name="OpenSumi"
  homepage="https://github.com/opensumi/core"
  supports="Tools"
>

OpenSumi 是一个帮助您快速构建 AI Native IDE 产品的框架。

**主要功能：**

- 在 OpenSumi 中支持 MCP 工具
- 支持内置 IDE MCP 服务器和自定义 MCP 服务器

</McpClient>

<McpClient
  name="oterm"
  homepage="https://github.com/ggozad/oterm"
  supports="Prompts, Tools, Sampling"
>

oterm 是 Ollama 的终端客户端，允许用户创建聊天/代理。

**主要功能：**

- 支持多个完全可定制的聊天会话，与 Ollama 连接并使用工具。
- 支持 MCP 工具。

</McpClient>

<McpClient
  name="Postman"
  homepage="https://postman.com/downloads"
  supports="Resources, Prompts, Tools, Discovery, Sampling, Elicitation, Apps"
>

Postman 是最受欢迎的 API 客户端，现在支持 MCP 服务器测试和调试。

**主要功能：**

- 全面支持所有主要 MCP 功能（工具、提示词、资源和订阅）
- 用于调试 MCP 功能的快速、无缝 UI
- MCP 配置集成（Claude、VSCode 等），用于快速首次体验测试 MCP
- 与历史记录、变量和集合集成，以便重用和协作

</McpClient>

<McpClient
  name="Proxyman"
  homepage="https://proxyman.com"
  supports="Tools"
  instructions="https://docs.proxyman.com/mcp"
>

Proxyman 是一个用于 HTTP 调试和网络监控的原生 macOS 应用。它现在包含一个 MCP 服务器，使 AI 助手（Claude、Cursor 和其他兼容 MCP 的工具）能够直接与 Proxyman 交互，以检查 HTTP 流量、创建调试规则并通过自然语言控制应用。

**主要功能：**

- **AI 驱动调试**：要求 AI 分析捕获的流量、查找特定请求或解释 API 响应
- **免手动规则创建**：通过对话创建断点、映射本地/远程规则
- **流量检查工具**：获取流、流详情、导出 cURL 命令，并使用多个标准过滤流量
- **会话控制**：清除会话、切换录制和管理 SSL 代理域
- **安全设计**：仅限本地主机服务器，具有每会话令牌身份验证

**了解更多：**

- [Proxyman MCP 文档](https://docs.proxyman.com/mcp)
- [Proxyman 网站](https://proxyman.com)

</McpClient>

<McpClient
  name="Qoder"
  homepage="https://www.qoder.com/"
  supports="Tools"
  instructions="https://docs.qoder.com/user-guide/chat/model-context-protocol"
>

Qoder 是阿里巴巴推出的下一代代理编码平台，专为现实世界的软件开发而设计。通过结合增强的上下文工程与自主代理，它提供对超大型代码库的深度感知，并可以支持从副驾驶辅助到完全自主编码的工作流。

**主要功能：**

- **代理模式**：高效单代理协作，根据项目上下文自主决定操作，包括跨文件重构、调试和功能迭代。
- **专家模式**：多代理编排，分解复杂需求并委托给虚拟专家团队（设计、实现、测试、QA）进行并行执行。
- **任务模式**：完全自主的端到端编码，从目标定义到需求澄清、规划、执行和验证，并提供全面的最终报告。
- **工程知识引擎**：由 Repo Wiki 驱动的架构理解，使代理具有完整的代码库感知并与项目标准保持一致。
- **记忆引擎**：持久记忆开发者偏好、项目约定和历史交互，以随时间提高一致性。

</McpClient>

<McpClient
  name="RecurseChat"
  homepage="https://recurse.chat"
  supports="Tools"
>

RecurseChat 是一个强大、快速、本地优先的聊天客户端，支持 MCP。RecurseChat 支持多种 AI 提供商，包括 LLaMA.cpp、Ollama 和 OpenAI、Anthropic。

**主要功能：**

- 本地 AI：支持使用 Ollama 模型的 MCP。
- MCP 工具：单个 MCP 服务器管理。轻松可视化 MCP 服务器的连接状态。
- MCP 导入：从 Claude Desktop 应用或 JSON 导入配置

**了解更多：**

- [RecurseChat 文档](https://recurse.chat/docs/features/mcp/)

</McpClient>

<McpClient
  name="Replit"
  homepage="https://replit.com/products/agent"
  supports="Tools, DCR"
>

Replit Agent 是一个 AI 驱动的软件开发工具，通过自然语言构建和部署应用程序。它支持 MCP 集成，使用户能够通过自定义工具和数据源扩展代理的功能。

**了解更多：**

- [Replit MCP 文档](https://docs.replit.com/replitai/mcp/overview)
- [MCP 安装链接](https://docs.replit.com/replitai/mcp/install-links)

</McpClient>

<McpClient
  name="Roo Code"
  homepage="https://roocode.com"
  supports="Resources, Tools"
  instructions="https://docs.roocode.com/features/mcp/using-mcp-in-roo"
>

Roo Code 通过 MCP 实现 AI 编码辅助。

**主要功能：**

- 支持 MCP 工具和资源
- 与开发工作流集成
- 可扩展的 AI 功能

</McpClient>

<McpClient
  name="rtrvr.ai"
  homepage="https://rtrvr.ai"
  supports="Tools"
  instructions="https://www.rtrvr.ai/docs/tool-calling"
>
[rtrvr.ai](https://rtrvr.ai) 是 AI Web 代理 Chrome 扩展，可自主运行复杂的浏览器工作流，将数据检索到 Sheets，并调用 API/MCP 服务器——只需提示并在您自己的浏览器内即可完成！

**主要功能：**

- 浏览器内轻松的 MCP 集成：只需打开 Chrome 扩展，添加服务器 URL，并以网页为背景提示服务器调用！
- 通过将浏览器转变为 MCP 服务器来远程控制您的浏览器：只需将 MCP URL 复制/粘贴到任何 MCP 客户端（无需 npx），并触发代理浏览器工作流！
- 提示我们的代理执行结合 Web 代理操作与 MCP 工具调用的工作流；在 Web 上找到某人的电子邮件，然后通过 Zapier MCP 给他们发送电子邮件。
- 可重用和可调度的自动化：运行工作流后，轻松重新运行或安排计划在后台执行，而您可以在浏览器中执行其他任务。

</McpClient>
<McpClient
  name="Shortwave"
  homepage="https://www.shortwave.com"
  supports="Tools"
>

Shortwave 是一个 AI 驱动的电子邮件客户端，支持 MCP 工具以增强电子邮件生产力和工作流自动化。

**主要功能：**

- 用于增强电子邮件工作流的 MCP 工具集成
- 丰富的 UI，用于添加、管理和与各种 MCP 服务器交互
- 支持远程（Streamable HTTP 和 SSE）和本地（Stdio）MCP 服务器
- AI 辅助管理您的电子邮件、日历、任务和其他第三方服务

</McpClient>

<McpClient
  name="Simtheory"
  homepage="https://simtheory.ai"
  supports="Resources, Prompts, Tools, Discovery"
>

Simtheory 是一个代理 AI 工作空间，在单个订阅下统一多个 AI 模型、工具和功能。它通过其 MCP Store 提供全面的 MCP 支持，允许用户使用生产力工具和集成扩展其工作空间。

**主要功能：**

- **MCP Store**：生产力工具和 MCP 服务器集成的市场
- **并行任务**：在 MCP 工具支持下同时运行多个 AI 任务
- **模型目录**：访问具有 MCP 工具集成的前沿模型
- **托管 MCP 服务器**：即插即用 MCP 集成，无需技术设置
- **高级 MCP**：专用工具，如 Tripo3D（3D 创作）、播客制作器和视频制作器
- **企业就绪**：具有 MCP 工具细粒度访问控制的灵活工作空间

**了解更多：**

- [Simtheory 网站](https://simtheory.ai)

</McpClient>

<McpClient
  name="Slack MCP Client"
  homepage="https://github.com/tuannvm/slack-mcp-client"
  supports="Tools"
>

Slack MCP Client 充当 Slack 和模型上下文协议 (MCP) 服务器之间的桥梁。使用 Slack 作为界面，它使大型语言模型 (LLM) 能够通过标准化 MCP 工具连接并与各种 MCP 服务器交互。

**主要功能：**

- **支持流行的 LLM 提供商：** 与领先的的大型语言模型提供商（如 OpenAI、Anthropic 和 Ollama）无缝集成，允许用户在 Slack 内利用先进的对话 AI 和编排能力。
- **动态和安全集成：** 支持动态注册 MCP 工具，适用于频道和直接消息，并通过环境变量或 Kubernetes 密钥安全管理凭据。
- **易于部署和扩展：** 提供官方 Docker 镜像、Kubernetes 的 Helm chart 和用于本地开发的 Docker Compose，使其易于部署、配置和扩展其他 MCP 服务器或工具。

</McpClient>

<McpClient
  name="Smithery Playground"
  homepage="https://smithery.ai/playground"
  supports="Resources, Prompts, Tools"
>

Smithery Playground 是一个开发者优先的 MCP 客户端，用于探索、测试和针对 LLM 调试 MCP 服务器。它提供详细的 MCP RPC 跟踪以帮助排查实现问题。

**主要功能：**

- 通过 URL 或从 Smithery 的注册表一键连接到 MCP 服务器
- 开发在 localhost 上运行的 MCP 服务器
- 检查工具、提示词、资源和采样配置，并提供实时预览
- 运行对话或原始工具调用以在发布前验证 MCP 行为
- 全面支持 OAuth MCP 规范

</McpClient>

<McpClient
  name="SpinAI"
  homepage="https://docs.spinai.dev"
  supports="Tools"
>

SpinAI 是一个用于构建可观察 AI 代理的开源 TypeScript 框架。该框架提供原生 MCP 兼容性，允许代理无缝集成 MCP 服务器和工具。

**主要功能：**

- 为 AI 代理内置 MCP 兼容性
- 开源 TypeScript 框架
- 可观察的代理架构
- 原生支持 MCP 工具集成

</McpClient>

<McpClient
  name="Superinterface"
  homepage="https://superinterface.ai"
  supports="Tools"
>

Superinterface 是 AI 基础设施和开发者平台，用于构建应用内 AI 助手，支持 MCP、交互式组件、客户端函数调用等。

**主要功能：**

- 在通过 React 组件或脚本标签嵌入的助手中使用来自 MCP 服务器的工具
- 支持 SSE 传输
- 使用来自任何 AI 提供商的任何 AI 模型（OpenAI、Anthropic、Ollama、其他）

</McpClient>

<McpClient
  name="Superjoin"
  homepage="https://superjoin.ai"
  supports="Tools"
>

Superjoin 将 MCP 的力量直接带入 Google Sheets 扩展。使用 Superjoin，用户无需离开电子表格即可访问和调用 MCP 工具和代理，从而在数据所在之处实现强大的 AI 工作流和自动化。

**主要功能：**

- 原生 Google Sheets 加载项，提供对 MCP 功能的轻松访问
- 支持 OAuth 2.1 和基于 header 的身份验证，以实现安全和灵活的连接
- 兼容 SSE 和 Streamable HTTP 传输，以实现高效的实时流式通信
- 完全基于网页、跨平台的客户端，无需额外软件安装

</McpClient>

<McpClient
  name="Swarms"
  homepage="https://github.com/kyegomez/swarms"
  supports="Tools, Discovery"
>

Swarms 是一个生产级多代理编排框架，支持 MCP 集成以实现动态工具发现和执行。

**主要功能：**

- 通过 SSE 传输连接到 MCP 服务器以实现实时工具集成
- 自动从 MCP 服务器发现和加载工具
- 支持跨多个代理的分布式工具功能
- 企业就绪，具有高可用性和可观察性功能
- 支持多个 AI 模型提供商的模块化架构

**了解更多：**

- [Swarms MCP 集成文档](https://docs.swarms.world/en/latest/swarms/tools/tools_examples/)

</McpClient>

<McpClient
  name="systemprompt"
  homepage="https://systemprompt.io"
  supports="Resources, Prompts, Tools, Sampling"
>

systemprompt 是一个语音控制的移动应用，用于管理您的 MCP 服务器。安全地从口袋中利用 MCP 代理。适用于 iOS 和 Android。

**主要功能：**

- **原生移动体验**：随时随地在 Android 和 iOS 设备上访问和管理您的 MCP 服务器
- **高级 AI 驱动语音识别**：复杂的语音识别引擎，通过尖端 AI 和自然语言处理 (NLP) 增强，专门 tuned 以理解复杂的开发者术语和命令结构
- **统一多 MCP 服务器管理**：轻松地从单个集中式移动应用管理和交互多个模型上下文协议 (MCP) 服务器

</McpClient>

<McpClient
  name="Tambo"
  homepage="https://tambo.co"
  supports="Prompts, Tools, Discovery, Sampling, Elicitation"
>

Tambo 是一个用于在 React 中构建自定义聊天体验的平台，具有集成的自定义用户界面组件。

**主要功能：**

- 托管平台，带有 React SDK，用于将聊天或其他基于 LLM 的体验集成到您自己的应用中。
- 支持在聊天体验中选择任意 React 组件，具有状态管理和工具调用。
- 支持 MCP 服务器，来自 Tambo 的服务器或直接来自浏览器。
- 支持 OAuth 2.1 和自定义基于 header 的身份验证。
- 支持 MCP 工具和采样，更多 MCP 功能即将推出。

</McpClient>

<McpClient
  name="Tencent CloudBase AI DevKit"
  homepage="https://docs.cloudbase.net/ai/agent/mcp"
  supports="Tools"
>

Tencent CloudBase AI DevKit 是一个用于在几分钟内构建 AI 代理的工具，具有零代码工具、安全数据集成和通过 MCP 扩展的插件。

**主要功能：**

- 支持 MCP 工具
- 使用 MCP 服务器扩展代理
- MCP 服务器托管：无服务器托管和身份验证支持

</McpClient>

<McpClient
  name="TheiaAI/TheiaIDE"
  homepage="https://eclipsesource.com/blogs/2024/10/07/introducing-theia-ai/"
  supports="Tools"
>

Theia AI 是一个用于构建 AI 增强工具和 IDE 的框架。[AI 驱动的 Theia IDE](https://eclipsesource.com/blogs/2024/10/08/introducting-ai-theia-ide/) 是一个基于 Theia AI 构建的开放灵活的开发环境。

**主要功能：**

- **工具集成**：Theia AI 使 AI 代理（包括 Theia IDE 中的代理）能够利用 MCP 服务器进行无缝工具交互。
- **可定制提示词**：Theia IDE 允许用户定义和调整提示词，动态集成 MCP 服务器以定制工作流。
- **自定义代理**：Theia IDE 支持创建利用 MCP 功能的自定义代理，使用户能够即时设计专用工作流。

Theia AI 和 Theia IDE 的 MCP 集成为用户提供灵活性，使其成为探索和适应 MCP 的强大平台。

**了解更多：**

- [Theia IDE 和 Theia AI MCP 公告](https://eclipsesource.com/blogs/2024/12/19/theia-ide-and-theia-ai-support-mcp/)
- [下载 AI 驱动的 Theia IDE](https://theia-ide.org/)

</McpClient>

<McpClient
  name="Tome"
  homepage="https://github.com/runebookai/tome"
  supports="Tools"
>

Tome 是一个开源跨平台桌面应用，专为使用本地 LLM 和 MCP 服务器而设计。它旨在对初学者友好，并为刚开始使用 MCP 的人抽象掉配置的繁琐细节。

**主要功能：**

- MCP 服务器由 Tome 管理，因此无需安装 uv 或 npm 或配置 JSON
- 用户可以通过 UI 快速添加或删除 MCP 服务器
- 任何支持工具的 Ollama 本地模型都兼容

</McpClient>

<McpClient
  name="TypingMind App"
  homepage="https://www.typingmind.com"
  supports="Tools"
  instructions={[
    ["公共服务器", "https://docs.typingmind.com/model-context-protocol-(mcp)-in-typingmind"],
    ["私有服务器", "https://docs.typingmind.com/model-context-protocol-(mcp)-in-typingmind/use-mcp-with-private-mcp-connector"]
  ]}
>

TypingMind 是一个具有 MCP 支持的 LLM 高级前端。TypingMind 支持所有流行的 LLM 提供商，如 OpenAI、Gemini、Claude，用户可以使用自己的 API 密钥。

**主要功能：**

- **MCP 工具集成**：配置 MCP 后，MCP 工具将显示为插件，可以通过主应用界面轻松启用/禁用。
- **将 MCP 工具分配给代理**：TypingMind 允许用户创建分配了一组 MCP 服务器的 AI 代理。
- **远程 MCP 服务器**：允许用户通过其 MCP 连接器配置自定义运行 MCP 服务器的位置，允许跨多个设备（笔记本电脑、移动设备等）使用 MCP 工具或从远程私有服务器控制 MCP 服务器。

**了解更多：**

- [TypingMind MCP 文档](https://www.typingmind.com/mcp)
- [下载 TypingMind (PWA)](https://www.typingmind.com/)

</McpClient>

<McpClient
  name="v0"
  homepage="https://v0.app"
  supports="Tools"
  instructions="https://v0.app/docs/MCP"
>

v0 将您的想法转化为全栈应用，无需代码。用自然语言描述您想要的内容，v0 为您构建。v0 可以搜索网页、检查站点、自动修复错误并与外部工具集成。

**主要功能：**

- **视觉到代码**：从您的线框图或模型创建高保真 UI
- **一键部署**：一键部署到安全、可扩展的基础设施
- **网页搜索**：搜索网页获取当前信息并获得引用结果
- **站点检查器**：检查网站以了解其结构和内容
- **自动错误修复**：通过智能诊断自动修复代码中的错误
- **MCP 集成**：从 Vercel Marketplace 连接 MCP 服务器以实现零配置设置，或添加您自己的自定义 MCP 服务器

**了解更多：**

- [v0 网站](https://v0.app)

</McpClient>

<McpClient
  name="VS Code GitHub Copilot"
  homepage="https://code.visualstudio.com/"
  supports="Resources, Prompts, Tools, Discovery, Sampling, Roots, Elicitation, Instructions, Apps, CIMD, DCR"
  instructions="https://code.visualstudio.com/docs/copilot/customization/mcp-servers"
>

VS Code 通过 [代理模式](https://code.visualstudio.com/docs/copilot/chat/chat-agent-mode) 将 MCP 与 GitHub Copilot 集成，允许在您的代理编码工作流中直接与 MCP 提供的工具交互。在 Claude Desktop、工作区或用户设置中配置服务器，具有 guided MCP 安装并在输入变量中安全处理密钥以避免泄露硬编码密钥。

**主要功能：**

- 支持 stdio 和服务器发送事件 (SSE) 传输
- 每个代理会话每会话选择工具以获得最佳性能
- 简单的服务器调试，具有重启命令和输出日志
- 具有可编辑输入和始终允许切换的工具调用
- 与现有 VS Code 扩展系统集成，从扩展注册 MCP 服务器

</McpClient>

<McpClient
  name="VT Code"
  homepage="https://github.com/vinhnx/vtcode"
  supports="Resources, Prompts, Tools, Discovery, Sampling (partial), Roots, Elicitation"
>

VT Code 是一个终端编码代理，与模型上下文协议 (MCP) 服务器集成，专注于可预测的工具权限和强大的传输控制。

**主要功能：**

- 通过 stdio 连接到 MCP 服务器；可选实验性 RMCP/streamable HTTP 支持
- 通过 `vtcode.toml` 配置每个提供商的并发性、启动/工具超时和重试
- 基于模式的工具、资源和提示词允许列表，具有提供商级覆盖

**了解更多：**

- [MCP 集成指南](https://github.com/vinhnx/vtcode/blob/main/docs/guides/mcp-integration.md)

</McpClient>

<McpClient
  name="Warp"
  homepage="https://www.warp.dev/"
  supports="Resources, Tools, Discovery"
  instructions="https://docs.warp.dev/knowledge-and-collaboration/mcp"
>

Warp 是智能终端，内置 AI 和您的开发团队的知识。通过将自然语言功能直接集成到代理命令行中，Warp 使开发人员能够更高效地编码、自动化和协作——所有都在具有现代 UX 的终端内。

**主要功能：**

- **支持 MCP 的代理模式**：使用自然语言提示词调用工具并访问来自 MCP 服务器的数据
- **灵活的服务器管理**：通过 Warp 的内置 UI 添加和管理基于 CLI 或 SSE 的 MCP 服务器
- **实时工具/资源发现**：查看每个运行中的 MCP 服务器的工具和资源
- **可配置启动**：设置 MCP 服务器随 Warp 自动启动或根据需要手动启动

</McpClient>

<McpClient
  name="WhatsMCP"
  homepage="https://wassist.app/mcp/"
  supports="Tools"
>

WhatsMCP 是 WhatsApp 的 MCP 客户端。WhatsMCP 让您可以在 WhatsApp 聊天的舒适环境中与您的 AI 栈交互。

**主要功能：**

- 支持 MCP 工具
- SSE 传输，全面支持 OAuth2
- WhatsApp 消息的聊天流管理
- 一键设置连接到您的 MCP 服务器
- 聊天内管理 MCP 服务器
- WhatsApp 中原生支持 OAuth 流

</McpClient>

<McpClient
  name="Windsurf Editor"
  homepage="https://codeium.com/windsurf"
  supports="Tools, Discovery"
  instructions={[
    ["指南", "https://docs.windsurf.com/windsurf/cascade/mcp"],
    ["视频教程", "https://windsurf.com/university/tutorials/configuring-first-mcp-server"]
  ]}
>

Windsurf Editor 是一个代理 IDE，将 AI 辅助与开发者工作流相结合。它具有创新的 AI Flow 系统，可在保持开发者控制的同时实现协作和独立的 AI 交互。

**主要功能：**

- 用于人机协作的革命性 AI Flow 范式
- 智能代码生成和理解
- 具有多模型支持的丰富开发工具

</McpClient>

<McpClient
  name="Witsy"
  homepage="https://github.com/nbonamy/witsy"
  supports="Tools"
>

Witsy 是一个 AI 桌面助手，支持 Anthropic 模型和 MCP 服务器作为 LLM 工具。

**主要功能：**

- 支持多个 MCP 服务器
- 用于执行命令和脚本的工具集成
- 本地服务器连接以增强隐私和安全
- 从 Smithery.ai 轻松安装
- 开源，适用于 macOS、Windows 和 Linux

</McpClient>

<McpClient
  name="Zed"
  homepage="https://zed.dev/docs/assistant/model-context-protocol"
  supports="Prompts, Tools"
  instructions="https://zed.dev/docs/ai/mcp"
>

Zed 是一个高性能代码编辑器，内置 MCP 支持，专注于提示词模板和工具集成。

**主要功能：**

- 提示词模板在编辑器中显示为斜杠命令
- 用于增强编码工作流的工具集成
- 与编辑器功能和工作区上下文紧密集成
- 不支持 MCP 资源

</McpClient>

<McpClient
  name="Zencoder"
  homepage="https://zencoder.ai"
  supports="Tools"
  instructions="https://docs.zencoder.ai/features/integrations-and-mcp#model-context-protocol-mcp"
>

Zencoder 是一个编码代理，可作为 VS Code 和 JetBrains 系列 IDE 的扩展使用，满足开发人员在其 already work 的地方。它带有 RepoGrokking（深度上下文代码库理解）、代理管道，以及创建和共享自定义代理的能力。

**主要功能：**

- RepoGrokking - 代码库的深度上下文理解
- 代理管道 - 在输出代码之前运行、测试和执行代码
- Zen Agents 平台 - 能够构建和创建自定义代理并与团队共享
- 集成 MCP 工具库，一键安装
- 用于单元和 E2E 测试的专用代理

**了解更多：**

- [Zencoder 文档](https://docs.zencoder.ai)

</McpClient>

## 向您的应用程序添加 MCP 支持

如果您已向应用程序添加了 MCP 支持，我们鼓励您提交拉取请求以将其添加到此列表中。MCP 集成可以为用户提供强大的上下文 AI 能力，并使您的应用程序成为不断成长的 MCP 生态系统的一部分。

添加 MCP 支持的好处：

- 使用户能够自带上下文和工具
- 加入一个不断成长的可互操作 AI 应用程序生态系统
- 为用户提供灵活的集成选项
- 支持本地优先的 AI 工作流

要开始在您的应用程序中实现 MCP，请查看我们的 [Python](https://github.com/modelcontextprotocol/python-sdk) 或 [TypeScript SDK 文档](https://github.com/modelcontextprotocol/typescript-sdk)
